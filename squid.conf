# DNS configuration - use external DNS servers
dns_nameservers 8.8.8.8 8.8.4.4

# Allowed external domains
acl allowed dstdomain .github.com .npmjs.org .pypi.org .python.org .ubuntu.com .google.com

http_access allow allowed
http_access deny all

# Squid normally listens to port 3128
http_port 3128